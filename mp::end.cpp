#include<bits/stdc++.h>

using namespace std;

int main(){
    map<int,int>mp = {
        {1,2} , {2,3} , {5 , 6}
    };

    if(mp.find(3)==mp.end())cout << "lol";
    if(mp.find(3)==mp.end())cout << "lol";

}
